# 远期修正列表

> 记录已知但暂未修复的问题，按优先级排列，后续版本逐步处理。

---

## 待修正

（暂无）

---

## 已修正

### 1. 时区未对齐到东八区（Asia/Shanghai） — 2026-03-02 已修复

- 在 `time_utils.py` 新增 `USER_TIMEZONE`（UTC+8）、`get_local_now()`、`local_today_str()`、`local_yesterday_str()`
- Memory 全链路（manager/compressor/profile_builder/reader/router）、AI 聊天日期感知、memory_tools、日志文件命名 统一使用东八区
- omi 兼容层 API、遥测导出、数据库存储保持 UTC（这些场景 UTC 是正确的）
